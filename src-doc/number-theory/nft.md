Modint の原始根 `g` の設定を確認
<!--
- Modintを使う(原始根として`static`変数`G`を自分でセットアップする必要あり)
    - 原始根は$g^{\phi(p)/d}$がどれも1でないようなもので、$10^{16}$以下のmodで最大$417$なので$2$から順に試しても十分高速
- 998244353のとき$|a|,|b|\leq 2^{19}$以下のみ対応
-->
